---
const { image, title, slug } = Astro.props;
---

<a class="flex flex-col gap-4" href={`/articles/${slug}`}>
  <div class={`bg-zinc-100 aspect-square bg-cover bg-center`} style={`background-image: url(${image})`}>
    
  </div>
  <h3 class="text-3xl font-semibold tracking-tight">{title}</h3>
</a>